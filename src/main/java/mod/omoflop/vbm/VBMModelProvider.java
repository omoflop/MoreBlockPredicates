package mod.omoflop.vbm;

import org.jetbrains.annotations.Nullable;

import net.fabricmc.fabric.api.client.model.ModelProviderContext;
import net.fabricmc.fabric.api.client.model.ModelProviderException;
import net.fabricmc.fabric.api.client.model.ModelResourceProvider;
import net.minecraft.client.render.model.UnbakedModel;
import net.minecraft.util.Identifier;

public class VBMModelProvider /*implements ModelResourceProvider*/ {
    //public final DynamicModel DYN_MODEL = new DynamicModel(new Identifier("minecraft:block/acacia_stairs"));

    //@Override
    //public @Nullable UnbakedModel loadModelResource(Identifier identifier, ModelProviderContext context) throws ModelProviderException {       
        //if (identifier.equals(new Identifier("minecraft:block/emerald_block"))) {
        //    return DYN_MODEL;
        //}
    //    return null;
    //}

    //public static boolean replace(Identifier in, String namespace, String name) {
    //    return (in.equals(new Identifier(namespace, "block/"+name)) || in.equals(new Identifier(namespace, "item/"+name))) ;
    //}
}
